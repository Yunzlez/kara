package be.zlz.kara.bin.services.webhook;

import be.zlz.kara.bin.domain.Reply;
import org.springframework.stereotype.Service;

@Service
public class WebhookService {

    public Reply runWebhook() {
        return new Reply(

        );
    }
}
