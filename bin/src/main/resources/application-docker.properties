spring.datasource.url=jdbc:mysql://${DATABASE_URI}/kara?useSSL=false&serverTimezone=UTC
spring.datasource.username=${DATABASE_USR}
spring.datasource.password=${DATABASE_PWD}
spring.jpa.hibernate.ddl-auto=update
server.use-forward-headers=true

mqtt.broker.url=${MQTT_ENDPOINT:localhost}
mqtt.enabled=${MQTT_ENABLED:false}
mqtt.clientid=kara

logging.level.be.zlz.kara= INFO